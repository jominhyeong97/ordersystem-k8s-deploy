apiVersion: v1
kind: Service
metadata:
  # 클러스터 내에서 service명은 url 주소로 사용된다.(중요) ex.http://my-service
  name: my-service
  namespace: minhyeong
spec:
  ports:
  # port는 service의 port, target port는 pod의 포트다.
  - port: 80
    targetPort: 80
  selector:
    app: my-nginx